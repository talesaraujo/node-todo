version: "2"
services:
  app:
    container_name: todoapp
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/usr/app/
    links:
      - mongo

  mongo:
    container_name: todo_mongodb
    image: mongo:4.0.4
    volumes:
      - ./data:/data/db/
    ports:
      - "27017-27019:27017-27019"